# fix-ebec505905c31cdb — Fix: The prior 'External Dependencies' explicitly requires storing 'S3-compatible service for storing verification media' as a core dependency, but the CURRENT_DOC mentions S3 only as an optional fallback ('if MEDIA_BUCKET is set') and defaults to local volume, deviating from the authoritative upstr

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

missing: The prior 'External Dependencies' explicitly requires storing 'S3-compatible service for storing verification media' as a core dependency, but the CURRENT_DOC mentions S3 only as an optional fallback ('if MEDIA_BUCKET is set') and defaults to local volume, deviating from the authoritative upstream requirement.

Severity: medium.