# fix-b4477a2072f90786 — Fix: The prior 'Database' section specifies a 'Users table with telegram_id, reputation_score, crypto_balance', but the CURRENT_DOC data model uses 'tg_id' instead of the mandated 'telegram_id' column name, constituting a missing element from the prior specification.

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

missing: The prior 'Database' section specifies a 'Users table with telegram_id, reputation_score, crypto_balance', but the CURRENT_DOC data model uses 'tg_id' instead of the mandated 'telegram_id' column name, constituting a missing element from the prior specification.

Severity: medium.