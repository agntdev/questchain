# fix-3c70e7e1efd83d50 — Fix: The prior 'Full Feature List' includes 'Invite specific users to verify challenge completions', but the CURRENT_DOC's flow in 4.3 step 1 uses 'the creator picks a single verifier' or 'auto-assigned from recent interactors', not an explicit 'invite specific users' mechanism; there is no flow for

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

missing: The prior 'Full Feature List' includes 'Invite specific users to verify challenge completions', but the CURRENT_DOC's flow in 4.3 step 1 uses 'the creator picks a single verifier' or 'auto-assigned from recent interactors', not an explicit 'invite specific users' mechanism; there is no flow for the participant to send an invite to a specific user to be a verifier, only for the creator to pick, missing the feature as described.

Severity: medium.