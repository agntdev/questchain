# fix-a596ff7e99a03658 — Fix: The prior 'Challenge' entity includes 'reward_type/amount' but also implies an explicit 'status' field is required for the overall challenge lifecycle; the CURRENT_DOC's 'status' is present, but the prior also expects 'deadline' as a field; the CURRENT_DOC used 'ends_at' but the prior explicitl

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

missing: The prior 'Challenge' entity includes 'reward_type/amount' but also implies an explicit 'status' field is required for the overall challenge lifecycle; the CURRENT_DOC's 'status' is present, but the prior also expects 'deadline' as a field; the CURRENT_DOC used 'ends_at' but the prior explicitly names 'deadline' in the database table, thus missing the explicit 'deadline' column.

Severity: medium.