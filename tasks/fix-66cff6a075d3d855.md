# fix-66cff6a075d3d855 — Fix: The prior 'VerificationEvidence' entity requires a 'timestamp', but the CURRENT_DOC 'evidence' table uses 'submitted_at' and does not explicitly map or state 'timestamp', missing the explicit field.

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

missing: The prior 'VerificationEvidence' entity requires a 'timestamp', but the CURRENT_DOC 'evidence' table uses 'submitted_at' and does not explicitly map or state 'timestamp', missing the explicit field.

Severity: medium.