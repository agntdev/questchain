# fix-796bee6cd8003877 — Fix: The prior 'VerificationEvidence' table explicitly includes 'media_url' and 'timestamp', but the CURRENT_DOC splits into 'media_url' and 'text_body' and uses 'submitted_at' instead of 'timestamp', missing the direct 'timestamp' column.

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

missing: The prior 'VerificationEvidence' table explicitly includes 'media_url' and 'timestamp', but the CURRENT_DOC splits into 'media_url' and 'text_body' and uses 'submitted_at' instead of 'timestamp', missing the direct 'timestamp' column.

Severity: medium.