# fix-fa446c5aecbdf58d — Fix: The prior 'Sessions table' explicitly includes 'verified_status' and 'current_streak', which are present, but the upstream also implicitly requires a 'user_id, challenge_id' relationship; CURRENT_DOC uses 'user_tg_id' and 'challenge_id', missing the 'user_id' column name as specified.

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

missing: The prior 'Sessions table' explicitly includes 'verified_status' and 'current_streak', which are present, but the upstream also implicitly requires a 'user_id, challenge_id' relationship; CURRENT_DOC uses 'user_tg_id' and 'challenge_id', missing the 'user_id' column name as specified.

Severity: medium.