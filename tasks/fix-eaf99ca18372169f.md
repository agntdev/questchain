# fix-eaf99ca18372169f — Fix: The prior 'External Dependencies' requires 'Blockchain Integration: Crypto reward staking/transfer (ERC-20 compatible tokens), Smart contract for prize pool management (optional)', while the CURRENT_DOC in section 6.4 and the 'Blockchain' entry states it is 'out of scope for v1' and 'on-chain p

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 QST

contradiction: The prior 'External Dependencies' requires 'Blockchain Integration: Crypto reward staking/transfer (ERC-20 compatible tokens), Smart contract for prize pool management (optional)', while the CURRENT_DOC in section 6.4 and the 'Blockchain' entry states it is 'out of scope for v1' and 'on-chain payouts are a v2 feature', directly contradicting the upstream requirement that implies at least off-chain tracking must mirror on-chain staking/transfer, and the prior lists it as an essential dependency, not an optional future feature.

Severity: high.